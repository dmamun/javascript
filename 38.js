const person={
    name: 'mamun',
    age: 22,
    height: 5.5,
    hobbies: [ 'reading books', 'watching movies' ],
    study: 'bsc in cse',
    girlfriend: 'no',
    email: 'dmamun3311@gmail.com'
  }
  //for(let elements in person){
  //  console.log(`${elements}:${person[elements]}`);
  //}
  for(let elements of object.keys(person)){
    console.log(person[key]);
  }